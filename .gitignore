# content.json is now served directly by the page, so it IS committed.

.DS_Store

# Tooling and notes — kept on this machine, not published.
# The site only needs index.html, assets/, content.json and .nojekyll.
README.md
seal.py
content.example.json
apps-script/
assets/BACKDROP.md
assets/audio/README.md
